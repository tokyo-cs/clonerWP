# clonerWP
Converts WordPress sites into clean static HTML. A single PHP file: upload it to the hosting, run it from the browser — it lists every page from the database, renders and cleans each one, copies all assets, and outputs a deploy-ready ZIP. No dependencies, nothing to install.
